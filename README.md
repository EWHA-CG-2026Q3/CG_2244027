# CG_2244027 Unity assignments

Each assignment is a separate Unity project. In Unity Hub, use **Add project from disk** and select the assignment folder itself (not this repository root).

| Assignment | Unity project folder | Main scene |
| --- | --- | --- |
| S02 | `S02/` | `Assets/S02/Scenes/S02_Chair_Final.unity` |
| S03 | `S03/` | `Assets/S03/Scenes/S03_CustomPolygon_Star.unity` |
| S06 | `S06/` | Not assigned yet |

All projects use Unity 6.6 (`6000.6.0f1`). Each project includes its own `Assets`, `Packages`, and `ProjectSettings`. Unity regenerates `Library` and other temporary folders locally; those are intentionally not committed.
