# S05 출석인정과제 서술형

`new Color(r, g, b, a)`에서 `r`, `g`, `b`는 각각 빨강·초록·파랑의 세기를 0부터 1 사이의 값으로 나타내고, `a`는 투명도를 뜻한다.  
빨강은 `new Color(1f, 0f, 0f, 1f)`로 만든다.  
초록은 `new Color(0f, 1f, 0f, 1f)`로 만든다.  
파랑은 `new Color(0f, 0f, 1f, 1f)`로 만든다.
